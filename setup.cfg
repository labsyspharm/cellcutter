[metadata]
name = cellcutter
version = 0.1

[options]
packages = cellcutter
install_requires =
    scikit-image
    numpy
    pandas
    tifffile
    zarr
    numcodecs
python_requires = >= 3.7

[options.entry_points]
console_scripts =
    cut_cells = cellcutter.cli:cut
